pipeline {
    agent any
    stages {
        stage('startup') {
            steps {
                sh '''
                    echo "hello"
                '''
            }
        }
        stage(' Ansible-playbook') {
            steps {
                sh '''
                    ansible-playbook -i ansible/inventory ansible/playbook.yml 
                '''
            }
        }
    }
}
