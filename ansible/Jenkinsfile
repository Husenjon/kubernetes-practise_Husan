pipeline {
    agent any
    stages {
        stage('startup') {
            steps {
                sh '''
                    echo "Hello"
                '''
            }
        }
        stage(' Ansible-playbook') {
            steps {
                sh '''
                    ansible-playbook all -i ansible/inventory ansible/playbook.yml 
                '''
            }
        }
    }
}
