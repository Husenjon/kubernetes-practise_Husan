pipeline {
    agent any
    stages {
        stage('startup') {
            steps {
                sh '''
                    echo "Hello"
                '''
            }
        }
        stage(' Ansible-playbook') {
            steps {
                sh '''
                    ansible-playbook Worker -i ansible/inventory ansible/playbook.yml 
                '''
            }
        }
    }
}
